- hosts: all
  remote_user: root
  tasks:
  - name: install config nginx
    when: ansible_distribution_version > '7'
    tags: Install
    template: src=nginx.d/nginx.conf.jinja2 dest=/etc/nginx/nginx.conf
    notify: restart nginx

  handlers:
  - name: restart nginx
    service: name=nginx state=reloaded
